package com.example.easymarkets.data.repository

import javax.inject.Inject

class MainRepository @Inject constructor() {

    // normally those logics and providing data should be here but because of some limitations with the context,
    // I need to put them in the view model
    // It can be replaced later
}